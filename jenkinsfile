peline {
    agnet any 
    stages{
        stage("docker build image"){
            step{
                scrip (dockerImage = docker.build("myapp:${007}",".")){

                }
                

            }
        }
    }
}
